<% breadcrumb :users %>
<div class="container">
  <div class="contents-title">ユーザー一覧</div>
  <div class="users-index">
    <% @users.each do |user| %>
      <div class="users-index-item">
        <div class="user-left">
          <%= image_tag user.image.url %>
        </div>
        <div class="user-right">
          <%= link_to(user.name, "/users/#{user.id}") %>
        </div> 
      </div>
    <% end %>
  </div> 
</div> 